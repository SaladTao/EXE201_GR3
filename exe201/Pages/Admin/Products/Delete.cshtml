@page
@model exe201.Pages.Admin.Products.DeleteModel

@{
    ViewData["Title"] = "Delete Product";
    Layout = "~/Pages/Shared/_LayoutAdmin.cshtml";
}

<h1 class="display-6 text-danger mb-4">Delete Confirmation</h1>

<div class="alert alert-warning" role="alert">
    <h4 class="alert-heading">Are you sure you want to delete this product?</h4>
    <p>This action cannot be undone. Please review the product details below before confirming.</p>
</div>

<div class="card border-danger mb-4">
    <div class="card-header bg-danger text-white fw-bold">
        Product Details
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3 fw-bold">Name</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Product.Name)</dd>

            <dt class="col-sm-3 fw-bold">Description</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Product.Description)</dd>

            <dt class="col-sm-3 fw-bold">Price</dt>
            <dd class="col-sm-9 text-success">$@Html.DisplayFor(model => model.Product.Price)</dd>

            <dt class="col-sm-3 fw-bold">Image</dt>
            <dd class="col-sm-9">
                @if (!string.IsNullOrEmpty(Model.Product.ImageUrl))
                {
                    <img src="@Model.Product.ImageUrl" alt="Product Image" class="img-thumbnail" style="max-height: 200px;" />
                }
                else
                {
                    <span class="text-muted">No image available</span>
                }
            </dd>

            <dt class="col-sm-3 fw-bold">360 View</dt>
            <dd class="col-sm-9">
                @if (!string.IsNullOrEmpty(Model.Product.Camera360Url))
                {
                    <a href="@Model.Product.Camera360Url" target="_blank">View 360°</a>
                }
                else
                {
                    <span class="text-muted">No 360° view</span>
                }
            </dd>

            <dt class="col-sm-3 fw-bold">Virtual Gift</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Product.VirtualGift)</dd>

            <dt class="col-sm-3 fw-bold">Created At</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Product.CreatedAt)</dd>

            <dt class="col-sm-3 fw-bold">Category</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Product.Category.Name)</dd>
        </dl>
    </div>
</div>

<form method="post" class="d-flex align-items-center gap-3">
    <input type="hidden" asp-for="Product.Id" />
    <button type="submit" class="btn btn-danger px-4">Confirm Delete</button>
    <a asp-page="./Index" class="btn btn-secondary">Cancel</a>
</form>
